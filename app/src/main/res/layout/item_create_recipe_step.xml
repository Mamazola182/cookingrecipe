<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Header with step number and remove button -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp">

            <TextView
                android:id="@+id/tv_step_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:text="Bước 1"
                android:textColor="#FF6B6B"
                android:textSize="14sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/btn_remove_step"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentEnd="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_close"
                android:tint="#FF6B6B"
                android:contentDescription="Xóa bước" />
        </RelativeLayout>

        <!-- Step content -->
        <EditText
            android:id="@+id/et_step_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Mô tả chi tiết các thao tác trong bước này"
            android:background="@null"
            android:minLines="2"
            android:gravity="top"
            android:textSize="14sp"
            android:layout_marginBottom="8dp" />

        <!-- Image upload placeholder -->
        <RelativeLayout
            android:id="@+id/image_container"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:background="@drawable/bg_upload_image_small"
            android:layout_marginBottom="8dp">

            <ImageView
                android:id="@+id/iv_step_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:visibility="gone" />

            <LinearLayout
                android:id="@+id/upload_placeholder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:gravity="center"
                android:orientation="vertical">

                <ImageView
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/ic_image"
                    android:tint="#CCCCCC" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="Thêm ảnh minh họa"
                    android:textColor="#999999"
                    android:textSize="12sp" />
            </LinearLayout>
        </RelativeLayout>

        <!-- Duration input -->
        <EditText
            android:id="@+id/et_step_duration"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Thời gian thực hiện (phút)"
            android:background="@null"
            android:inputType="number"
            android:textSize="13sp"
            android:drawableStart="@drawable/ic_clock"
            android:drawablePadding="8dp"
            android:drawableTint="#999999" />
    </LinearLayout>
</androidx.cardview.widget.CardView>