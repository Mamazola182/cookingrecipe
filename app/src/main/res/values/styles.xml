<resources>

    <!-- Các style mặc định có sẵn -->
    <style name="Theme.MyApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Custom màu hoặc font nếu có -->
    </style>

    <!-- 🧩 Thêm hai style cho profile -->
    <style name="ProfileOptionStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingVertical">14dp</item>
        <item name="android:paddingHorizontal">12dp</item>
        <item name="android:background">@drawable/option_bg</item>
        <item name="android:layout_marginTop">8dp</item>
    </style>

    <style name="ProfileOptionText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#000000</item>
    </style>

</resources>
