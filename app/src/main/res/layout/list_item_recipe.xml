<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_card"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Image Container with Gradient Overlay -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="180dp">

            <ImageView
                android:id="@+id/item_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Recipe Image"
                android:scaleType="centerCrop" />

            <!-- Top Bar: Difficulty Badge & Favorite Button -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="top"
                android:layout_margin="12dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Favorite Button -->
                <ImageButton
                    android:id="@+id/item_favorite_button"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:background="@drawable/favorite_button_bg"
                    android:src="@drawable/ic_favorite_border"
                    android:contentDescription="Add to favorites"
                    android:elevation="4dp"
                    android:scaleType="centerInside"
                    android:padding="8dp"
                    app:tint="#FFFFFF" />
            </LinearLayout>
        </FrameLayout>

        <!-- Content Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp">

                <TextView
                    android:id="@+id/item_title"
                    android:layout_width="0dp" android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:text="Tên công thức"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="#212121"
                    android:maxLines="2"
                    android:ellipsize="end"
                    app:layout_constraintStart_toStartOf="parent" app:layout_constraintTop_toTopOf="parent"       app:layout_constraintBottom_toBottomOf="parent" app:layout_constraintEnd_toStartOf="@id/item_difficulty" /> <TextView
                android:id="@+id/item_difficulty"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:background="@drawable/difficulty_badge_bg"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:text="🔥 Dễ"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold"
                android:elevation="4dp"
                android:gravity="center"
                android:maxWidth="80dp"
                app:layout_constraintEnd_toEndOf="parent"     app:layout_constraintTop_toTopOf="@id/item_title"   app:layout_constraintBottom_toBottomOf="@id/item_title" /> </androidx.constraintlayout.widget.ConstraintLayout>

            <!-- Author with Icon -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_person"
                    android:contentDescription="Author icon"
                    app:tint="#757575" />

                <TextView
                    android:id="@+id/item_author"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:text="Tác giả"
                    android:textSize="13sp"
                    android:textColor="#757575"
                    android:maxLines="1"
                    android:ellipsize="end" />
            </LinearLayout>

            <!-- Favorite Count with Icon -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_favorite"
                    android:contentDescription="Favorite icon"
                    app:tint="#FF6B6B" />

                <TextView
                    android:id="@+id/item_fav_count"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:text="0"
                    android:textSize="13sp"
                    android:textColor="#757575" />
            </LinearLayout>

            <!-- Detail Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/item_detail_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="Xem chi tiết"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textAllCaps="false"
                app:cornerRadius="12dp"
                app:backgroundTint="#FF6B6B"
                app:elevation="0dp" />
        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>